CONFIG_ENV=config.env

# Kernel Source (Pixel 3a)
KERNEL_SOURCE=https://github.com/88recordsrd-create/kernel_google_msm-4.9_KernelSU
KERNEL_SOURCE_BRANCH=thirteen-kernelsu

# Configuración del kernel (sargo = Pixel 3a)
KERNEL_CONFIG_NAME=sargo_defconfig
KERNEL_CONFIG_CUSTOM=false

# SUSFS (para ocultar root)
SUSFS_SOURCE_BRANCH=kernel-4.9
ENABLE_SUSFS=true

# KernelSU-Next (modo legacy)
KSU_MODULE_SYSTEM=1
ENABLE_KERNELSU=true
ENABLE_KERNELSU_NONGKI_PATCH=false
KERNELSU_SETUP_SOURCE=https://raw.githubusercontent.com/rifsxd/KernelSU-Next/next/kernel/setup.sh
KERNELSU_TAG=next-susfs
KERNELSU_TYPE=KernelSU-Next

# Compilación (arm64)
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false
EXTRA_CMDS:LD=ld.lld

# Clang personalizado (funciona para kernel 4.9)
USE_CUSTOM_CLANG=true
CUSTOM_CLANG_SOURCE=https://github.com/LineageOS/android_prebuilts_clang_kernel_linux-x86_clang-r416183b.git
CUSTOM_CLANG_BRANCH=lineage-20.0
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# GCC (necesario para algunos módulos)
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true

# Configuraciones adicionales
DISABLE-LTO=false
DISABLE_CC_WERROR=false
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=false
REMOVE_UNUSED_PACKAGES=true

# AnyKernel3 (generará el zip flasheable)
USE_CUSTOM_ANYKERNEL3=false

# Ccache (acelera compilaciones futuras)
ENABLE_CCACHE=true

# No necesitamos dtbo ni boot.img separados
NEED_DTBO=false
BUILD_BOOT_IMG=false
